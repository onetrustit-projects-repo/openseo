apiVersion: v1
kind: Service
metadata:
  name: {{ include "openseo.fullname" . }}
  labels:
    {{- include "openseo.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "openseo.selectorLabels" . | nindent 4 }}
